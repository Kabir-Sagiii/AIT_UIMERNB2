var data = [101, "Raj", true, { city: "delhi", country: "india" }];

console.log(data);

console.log(data[3]);

//length property will tell us how many elements are present in the array
// <ArrayName>.length

var info = []; //empty array
console.log(info.length);
